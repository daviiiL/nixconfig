## Hosts

| Hosts     | dir    |
| --------- | ------ |
| framework | portal |
| desktop   | wndr   |

## file structure

```filesystem
.
└── hostname
    ├── configuration.nix
    ├── hardware-configuration.nix
    └── home.nix

```
